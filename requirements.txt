# TRACE Replay reproduction — modernized dependencies.
# Original pinned torch 2.0.1 / transformers 4.31 / CUDA 11.7 wheels (incompatible with CUDA 12.x cloud).
# Install torch FIRST for your CUDA version, e.g.:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#   (or cu121 / cu126 / cu128 depending on the machine)
transformers>=4.40,<4.47
deepspeed>=0.14
accelerate>=0.28
datasets>=2.18
evaluate>=0.4.1
peft>=0.10
sentencepiece>=0.1.99
protobuf>=4.25
tokenizers>=0.15
numpy
nltk>=3.8
rouge>=1.0.1
rapidfuzz>=3.0
sacrebleu
sacremoses
tensorboard
huggingface_hub>=0.20
ninja
qpth
quadprog
